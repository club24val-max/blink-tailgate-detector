services:
  - type: web
    name: blink-tailgate-detector
    env: python
    buildCommand: "apt-get update && apt-get install -y ffmpeg && pip install -r requirements.txt"
    startCommand: "uvicorn tailgate_detector_final:app --host 0.0.0.0 --port 10000"
